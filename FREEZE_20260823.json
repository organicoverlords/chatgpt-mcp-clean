{
  "freeze": "FREEZE_20260823.4",
  "frozen_at": "2026-08-23",
  "status": "verified-stable",
  "server_name": "shell-mcp",
  "repo_root": "C:\\Users\\Lauri\\AppData\\Local\\ChatGPTMcpClean",
  "entrypoint": "dist/index.js",
  "launcher": "start.ps1",
  "supervisor": "keepalive.ps1",
  "runtime": {
    "node": "v26.4.0",
    "npm": "11.17.0",
    "typescript": "7.0.2",
    "mcp_sdk": "1.24.3"
  },
  "binding": {
    "host": "127.0.0.1",
    "port": 3000,
    "note": "src/index.ts hard-fails on any other host/port. Freeing port 3000 is a precondition, not an option.",
    "public_origin": "https://kone.tailbf0440.ts.net",
    "exposure": "Tailscale Funnel proxies / -> http://127.0.0.1:3000"
  },
  "tools": [
    "execute_command",
    "start_process",
    "read_output",
    "kill_process",
    "busy_list",
    "busy_claim",
    "busy_release"
  ],
  "verification": {
    "smoke": {
      "command": "node scripts/smoke.mjs",
      "result": "PASS",
      "covers": [
        "oauth register/authorize/token/refresh",
        "initialize + tools/list exact 7-tool set",
        "foreground stdout+stderr+exit_code",
        "background immediate return + read_while_running",
        "two-job concurrency",
        "kill_process tree teardown (root+child gone)",
        "cross-session busy claim/list/release"
      ]
    },
    "stress": {
      "command": "node scripts/stress.mjs",
      "result": "PASS",
      "measured": {
        "burst": "100 concurrent tools/list, 0 errors, p99 384ms",
        "sustained": "400 req @ concurrency 25, 0 errors, ~381 req/s, p99 109ms",
        "exec": "12 concurrent execute_command real shell spawns, 0 errors",
        "leak_check": "active_requests returns to 0; RSS plateaus ~225MB across 1578 requests (V8 steady state, not a leak)"
      },
      "abuse_contract": {
        "unauthenticated": 401,
        "bad_bearer": 401,
        "malformed_json": 400,
        "body_over_4mb": 413,
        "unknown_tool": "JSON-RPC error",
        "GET /mcp": "405 + Allow: POST"
      }
    },
    "self_heal": "Killing the listener PID is recovered automatically by keepalive.ps1 in ~35s, with the new PID recorded to .state/keepalive/child.pid.",
    "cwd_contract": {
      "omitted": "uses server cwd",
      "absolute existing directory": "runs",
      "nonexistent": "working_directory does not exist: <path>",
      "relative": "working_directory must be an absolute path, received <value>",
      "path to a file": "working_directory is not a directory: <path>"
    },
    "concurrency": {
      "command": "node scripts/concurrency-busy.mjs 16",
      "result": "PASS",
      "covers": [
        "no lost updates across 16 processes",
        "exactly one winner for a contested scope",
        "stale lock reclaimed",
        "live lock fails fast instead of hanging"
      ]
    },
    "full_suite": {
      "smoke": "PASS",
      "stress": "PASS",
      "concurrency": "PASS",
      "run": "node scripts/smoke.mjs && node scripts/stress.mjs && node scripts/concurrency-busy.mjs 16"
    }
  },
  "defects_fixed_in_this_freeze": [
    {
      "id": "health-probe-identity",
      "file": "keepalive.ps1",
      "was": "Healthy() accepted any {\"status\":\"ok\"} on port 3000.",
      "impact": "A different server (codex-mcp-server) squatting port 3000 satisfied the probe, so the supervisor never started shell-mcp and pointed the public Funnel at the wrong process, producing a 401/400 storm and a 15-second failure-log loop.",
      "now": "Healthy() and PublicHealthy() additionally require name == 'shell-mcp'."
    },
    {
      "id": "port-reclaim-deadlock",
      "file": "keepalive.ps1",
      "was": "IsOwnedMcpListener() identified its own server only by reading the process CommandLine.",
      "impact": "An elevated listener reports a blank CommandLine to a non-elevated WMI query, so the supervisor classified its own server as foreign and refused to reclaim the port. Required manual admin intervention.",
      "now": "The listener PID is recorded to .state/keepalive/child.pid after a confirmed-healthy start and is treated as owned."
    },
    {
      "id": "supervisor-log-spam",
      "file": "keepalive.ps1",
      "was": "One log line per poll for a stuck condition, forever.",
      "impact": "~600 identical lines in 2 hours; real events were unreadable.",
      "now": "Consecutive identical messages collapse into a periodic 'repeated N times' heartbeat; supervisor.log rotates at 5MB; server.std*.log archives are pruned to the newest 5."
    },
    {
      "id": "no-restart-backoff",
      "file": "keepalive.ps1",
      "was": "A server that never becomes healthy was relaunched every 15s indefinitely.",
      "now": "Exponential backoff on consecutive failures, capped at 300s."
    },
    {
      "id": "busy_list-schema-dialect",
      "file": "src/server.ts, src/index.ts",
      "was": "busy_list omitted inputSchema, so it advertised {\"type\":\"object\",\"properties\":{}} with no $schema dialect while all six other tools declared draft-07.",
      "impact": "Suspected cause of a strict client surfacing all 7 tools and then disabling the connector on the first live busy_list call.",
      "now": "busy_list declares an explicit empty inputSchema and emits draft-07 like the rest. Because a declared schema makes the SDK reject an omitted `arguments` key, POST /mcp normalises tools/call arguments to {} when absent, so both call forms are accepted.",
      "caveat": "The client-side disable was never reproduced locally; busy_list always returned 200 over HTTP. The dialect mismatch is the leading hypothesis, not a confirmed root cause."
    },
    {
      "id": "stale-smoke-expectation",
      "file": "scripts/smoke.mjs",
      "was": "Asserted GET /mcp returns 200 + text/event-stream.",
      "impact": "The server is intentionally stateless POST-only and returns 405, so the assertion threw and the entire remainder of the suite never ran. No working regression signal existed.",
      "now": "Asserts the intended contract: 405 + Allow: POST."
    },
    {
      "id": "misleading-enoent-on-bad-working-directory",
      "file": "src/lib/process-manager.ts",
      "was": "normalizedCwd() returned workingDirectory unchecked and spawn() was given a cwd that may not exist.",
      "impact": "spawn() reports ENOENT when the *cwd* is missing and node attributes it to the executable, so any bad working_directory surfaced as 'spawn powershell.exe ENOENT'. This sent callers hunting a nonexistent PATH/PowerShell problem. Reproduced: nonexistent and relative working_directory both produced it while PowerShell resolved fine.",
      "now": "working_directory must be absolute, must exist, and must be a directory; each failure returns a message naming the real cause and the offending path. powershell.exe is additionally resolved through %SystemRoot% so a mangled inherited PATH cannot masquerade as the same error."
    },
    {
      "id": "supervisor-killed-by-locked-log",
      "file": "keepalive.ps1",
      "was": "ArchiveServerLogs() called Move-Item on the server's redirected stdout/stderr under $ErrorActionPreference='Stop'.",
      "impact": "The outgoing server's file handles are frequently still open at rotation, so Move-Item raised a sharing violation and terminated the entire supervisor. The server then stayed down with nothing to restart it. Reproduced directly: running keepalive.ps1 in the foreground died on 'Prosessi ei voi kayttaa tiedostoa'. This is the single largest source of unexplained MCP outages.",
      "now": "Archiving is best-effort per file and logs a skip. The whole poll body is additionally wrapped in try/catch so no transient error -- locked file, WMI hiccup, netstat blip -- can end supervision."
    },
    {
      "id": "busy-store-clobbers-other-writers",
      "file": "src/lib/busy-store.ts",
      "was": "load() ran only in the constructor; the in-memory map was authoritative for the process lifetime and persist() rewrote the whole file from it.",
      "impact": "A claim written by any other writer -- a second server instance, or an agent on a fallback path writing busy-claims.json directly -- was invisible to busy_list and was destroyed by the next persist(). For a mutual-exclusion primitive this is the worst available failure: two workers hold one scope and the evidence is erased. Observed live: a claim by chatgpt-orchestrator was absent from busy_list and then silently overwritten.",
      "now": "Every claim/list/release re-reads the store first. load() replaces the map rather than merging, so an out-of-band release is not resurrected; a missing file means no claims, while a parse or IO error leaves live claims untouched.",
      "verified": "Out-of-band claim is seen by busy_list, attributed correctly, refused to a different actor, survives on disk, and an out-of-band release is not resurrected."
    },
    {
      "id": "busy-store-lost-update-race",
      "file": "src/lib/busy-store.ts",
      "was": "Re-reading before each operation removed cross-writer divergence, but claim/list/release remained an unsynchronised read-modify-write.",
      "impact": "Two writers landing together could interleave and silently drop a claim -- the same lost-update class that erased the chatgpt-orchestrator claim, just on a narrower timing window.",
      "now": "Every claim/list/release runs under an O_EXCL lock file next to the store. A lock older than 15s is treated as abandoned and stolen so a process killed mid-write cannot wedge BUSY. Lock acquisition times out after 2s and throws a named error rather than blocking a tool call.",
      "verified": "16 processes / 16 distinct scopes: all 16 survive. 16 processes / 1 scope: exactly one winner, matching the actor on disk. Stale lock reclaimed in 78ms. Live lock fails fast, no hang. No latency cost: busy ops average 5.9ms."
    }
  ],
  "operational_notes": [
    "dist/ is gitignored but is what actually runs; a fresh clone needs `npm install && npm run build` before start.ps1 -SkipBuild is valid.",
    "dist/index.js was rebuilt from src at freeze time and verified byte-identical to the running binary.",
    ".env and .state/ are gitignored and contain the OAuth store, the legacy MCP path secret, and runtime claims. They are not in this repo and must be backed up separately.",
    "The previous MCP install lived under %LOCALAPPDATA%\\Temp and lost untracked files to Windows Storage Sense. This repo is deliberately outside Temp; do not relocate it back.",
    "Install supervisor persistence with: powershell -NoProfile -ExecutionPolicy Bypass -File scripts\\install-keepalive-task.ps1 (no admin required; -Uninstall removes it). Deliberately non-elevated: an elevated listener reports a blank CommandLine to a non-elevated WMI query, which breaks keepalive.ps1's port-reclaim ownership check."
  ],
  "supersedes": "FREEZE_20260823.3 (tag freeze-20260823.3)",
  "known_gaps": [],
  "persistence": {
    "scheduled_task": "ShellMcpKeepAlive",
    "state": "registered and verified",
    "principal": "KONE\\Lauri, RunLevel Limited (non-elevated), Interactive logon",
    "trigger": "at logon; RestartCount 3 at 1-minute intervals; no execution time limit",
    "install": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts\\install-keepalive-task.ps1",
    "uninstall": "same script with -Uninstall",
    "why_not_elevated": "The server binds 127.0.0.1:3000 and needs no admin. An elevated listener reports a blank CommandLine to a non-elevated WMI query, which is what made keepalive.ps1 misclassify its own server as foreign and refuse to reclaim the port.",
    "verified": "Whole stack killed (supervisor + server, port 3000 confirmed free), then Start-ScheduledTask rebuilt it end to end in ~4s: supervisor up, server healthy, child.pid recorded and matching the listener."
  }
}
